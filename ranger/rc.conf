# Ranger configuration file

# Enable mouse support (optional)
set mouse_enabled true

# Enable trash functionality using gio
map dD shell gio trash %s  # Move files to trash

# Optional: Set up a command to empty the trash
# You can create a custom command in commands.py for this

# Enable previewing of files (optional)
set preview_images true
set preview_images_method ueberzug
